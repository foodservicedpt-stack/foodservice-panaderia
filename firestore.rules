rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // La aplicación es pública y no requiere autenticación.
    // Cualquier persona con acceso a la URL puede leer y escribir datos.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
